# Elasticsearch — use URL (standard for Serverless) or Cloud ID (legacy)
ELASTICSEARCH_URL=https://your-deployment.es.region.cloud.es.io
# ELASTIC_CLOUD_ID=your-cloud-id          # alternative for classic Cloud deployments

# Elasticsearch API key
ELASTIC_API_KEY=your-api-key

# Jina AI (https://jina.ai/api-dashboard/)
JINA_API_KEY=your-jina-api-key

# Agent Builder (connector ID for the LLM used in Agent mode)
AGENT_CONNECTOR_ID=OpenAI-GPT-4-1-Mini
# KIBANA_URL is derived from ELASTICSEARCH_URL automatically (swap .es. for .kb.)
# Set explicitly only if the derivation doesn't work for your deployment:
# KIBANA_URL=https://your-deployment.kb.region.cloud.es.io
