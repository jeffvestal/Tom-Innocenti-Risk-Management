[pytest]
testpaths = notebooks/tests
markers =
    smoke: notebook execution smoke tests (mocked services)
    integration: tests requiring live Elastic + Jina credentials
addopts = -m "not integration"
timeout = 300
